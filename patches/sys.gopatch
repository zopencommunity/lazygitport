diff --git a/unix/ioctl_zos.go b/unix/ioctl_zos.go
index c8b2a750..5c44287a 100644
--- a/unix/ioctl_zos.go
+++ b/unix/ioctl_zos.go
@@ -20,6 +20,13 @@ func IoctlSetInt(fd int, req int, value int) error {
 	return ioctl(fd, req, uintptr(value))
 }
 
+// IoctlSetPointerInt performs an ioctl operation which sets an
+// integer value on fd, using the specified request number.
+func IoctlSetPointerInt(fd int, req int, value int) error {
+	v32 := int32(value)
+	return ioctlPtr(fd, req, unsafe.Pointer(&v32))
+}
+
 // IoctlSetWinsize performs an ioctl on fd with a *Winsize argument.
 //
 // To change fd's window size, the req argument should be TIOCSWINSZ.
